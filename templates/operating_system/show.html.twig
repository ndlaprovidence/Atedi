{% extends 'base.html.twig' %}

{% block title %}OperatingSystem{% endblock %}

{% block body %}
    <h1>OperatingSystem</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ operating_system.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ operating_system.title }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('operating_system_index') }}">back to list</a>

    <a href="{{ path('operating_system_edit', {'id': operating_system.id}) }}">edit</a>

    {{ include('operating_system/_delete_form.html.twig') }}
{% endblock %}
