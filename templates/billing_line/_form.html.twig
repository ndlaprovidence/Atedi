{{ form_start(form) }}

    <div class="row justify-content-md-center">
        <div class='col-4'>         
            <div class="input-group">
            {{ form_widget(form.price) }}
                <div class="input-group-append">
                    <span class="input-group-text">€</span>
                </div>
            </div>
        </div>
        <div class='col'>         
            {{ form_errors(form.title) }}
            {{ form_widget(form.title, {'attr': {'placeholder': 'Nouvelle ligne de facture'}}) }}
        </div>
        <div class='col-md-auto my-auto'>         
            <button class="green-btn px-2">{{ button_label|default('Valider') }}</button>
        </div>
    </div>

{{ form_end(form) }}
