<form method="post" action="{{ path('billing_line_delete', {'id': billing_line.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ billing_line.id) }}">
    <button class="btn">Delete</button>
</form>
